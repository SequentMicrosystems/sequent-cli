[package]
name = "sequent-cli"
version = "0.1.0"
edition = "2024"
default-run = "sequent-cli"

[dependencies]
clap = "4.5.47"
embedded-hal = "1.0.0"
linux-embedded-hal = "0.4.0"
paste = "1.0.15"
thiserror = "2.0.16"

[lints.rust]
unused = { level = "allow", priority = -1 } # dev only
